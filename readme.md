# TCP Messenger Client

Client for TCP Messenger.

### Usage

Commands:
- `/connect {IPv4|localhost} {port}` - connects to the specified server
- `/exit` - exits the program
- `/register {UserName}` - registers the user
- `/attach '{FilePath}'` - sends the file

To send a simple message just enter any text in the prompt.

To submit text/command press `Enter`
